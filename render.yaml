services:
  - type: web
    name: volidam-patir
    runtime: node
    buildCommand: npm install && npx prisma generate && npx prisma db push --accept-data-loss && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        value: postgresql://volidam_databse_user:K2IDqUSN2nSESXI9SfQyzQJsCldtTyxn@dpg-d6lhpv6a2pns73epf5c0-a/volidam_databse
      - key: JWT_SECRET
        value: volidam-patir-super-secret-jwt-key-2024
      - key: JWT_EXPIRES_IN
        value: 24h
      - key: NODE_ENV
        value: production
